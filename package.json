{
  "name": "hidden-catalyst",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "db:generate": "turbo db:generate",
    "db:migrate": "turbo db:migrate",
    "db:seed": "turbo db:seed",
    "engine:run": "pnpm --filter @hidden-catalyst/engine exec tsx src/cli.ts",
    "engine:test": "pnpm --filter @hidden-catalyst/engine test",
    "clean": "turbo clean"
  },
  "packageManager": "pnpm@9.0.0",
  "devDependencies": {
    "turbo": "^2.10.8"
  },
  "dependencies": {
    "pg": "^8.22.0"
  }
}
