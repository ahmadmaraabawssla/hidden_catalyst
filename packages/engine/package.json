{
  "name": "@hidden-catalyst/engine",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@hidden-catalyst/db": "workspace:*",
    "@hidden-catalyst/domain": "workspace:*",
    "@hidden-catalyst/connectors": "workspace:*"
  },
  "devDependencies": {
    "@hidden-catalyst/tsconfig": "workspace:*",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  }
}
