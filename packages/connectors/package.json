{
  "name": "@hidden-catalyst/connectors",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@hidden-catalyst/db": "workspace:*",
    "@hidden-catalyst/domain": "workspace:*"
  },
  "devDependencies": {
    "@hidden-catalyst/tsconfig": "workspace:*",
    "typescript": "^5.5.0"
  }
}
