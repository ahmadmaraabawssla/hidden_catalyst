{
  "name": "@hidden-catalyst/domain",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@hidden-catalyst/tsconfig": "workspace:*",
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  }
}
