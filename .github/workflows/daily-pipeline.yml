name: Daily Pipeline (00:01 Israel = 21:01 UTC)

on:
  schedule:
    # 21:01 UTC = 00:01 Israel (UTC+3)
    - cron: '1 21 * * *'
  workflow_dispatch:  # allow manual trigger

jobs:
  pipeline:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v2
        with:
          version: 8

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - run: pnpm install --no-frozen-lockfile

      - name: Refresh market caps (cheap, no AI)
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          FMP_API_KEY: ${{ secrets.FMP_API_KEY }}
        run: node scripts/fmp-updater.js

      - name: Run source-agnostic engine (harvest + AI, once/day)
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          FMP_API_KEY: ${{ secrets.FMP_API_KEY }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          SEC_USER_AGENT: "Hidden Catalyst Research contact@hiddencatalyst.com"
        run: pnpm engine:run
